package repository

// VoteRepository defines the interface for vote data access.
type VoteRepository interface {
	// Create(ctx context.Context, vote *model.Vote) error
	// FindByPollID(ctx context.Context, pollID string) ([]model.Vote, error)
	// GetTally(ctx context.Context, pollID string) ([]model.VoteTally, error)
	// ExistsByVoter(ctx context.Context, pollID, voterID string) (bool, error)
	// ExistsByIPHash(ctx context.Context, pollID, ipHash string) (bool, error)
}
